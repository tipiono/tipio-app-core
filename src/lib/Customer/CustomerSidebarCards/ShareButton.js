import React from 'react';
import SecondaryButton from '../../UI/Buttons/SecondaryButton';

const ShareButton = (props) => {
    return (
        <>
            <SecondaryButton text="Del nå" {...props} />
        </>
    )
};

export default ShareButton;
